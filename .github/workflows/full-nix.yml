# Copyright 2025 l3montree GmbH.
# SPDX-License-Identifier: AGPL-3.0

# This file is automatically generated. Do not edit directly.
on:
  workflow_call:
    inputs:
      devguard-api-url:
        description: The url of the API to send the scan request to (default 'https://api.devguard.org')
        type: string
        required: false
        default: https://api.devguard.org
      devguard-asset-name:
        description: DevGuard asset name (e.g., @opencode/projects/oci/assets/k8s-tools)
        type: string
        required: false
        default: $DEVGUARD_ASSET_NAME
      image-name:
        description: Full OCI image name without tag (e.g. ghcr.io/org/repo/image)
        type: string
        required: false
      nix-cache-substituter:
        description: Nix binary cache substituter URL. Set to empty string to disable.
        type: string
        required: false
        default: ""
      nix-cache-public-key:
        description: Trusted public key for the Nix binary cache.
        type: string
        required: false
        default: ""
      nix-cache-s3-endpoint:
        description: S3 API endpoint for pushing to the cache (e.g. s3.garage.l3montree.cloud). Leave empty to skip pushing.
        type: string
        required: false
        default: ""
      nix-cache-s3-bucket:
        description: S3 bucket name for the cache.
        type: string
        required: false
        default: ""
      nix-cache-region:
        description: S3 region for the cache bucket.
        type: string
        required: false
        default: ""
      nix-impure:
        description: Whether to allow impure builds (e.g., reading environment variables during build).
        type: string
        required: false
        default: "false"
      nix-version:
        description: Pinned Nix version for deterministic builds (must match other CI systems)
        type: string
        required: false
        default: 2.34.4
      allow-failure:
        description: Whether the job is allowed to fail without stopping the pipeline
        type: boolean
        required: false
        default: false
      commit-ref:
        description: Current commit reference
        type: string
        required: false
        default: ${{ github.ref_name }}
      is-tag:
        description: Is the current commit a tag
        type: string
        required: false
        default: ${{ github.ref_type == 'tag' }}
      devguard-web-ui:
        description: The DevGuard Web-UI Instance URL
        type: string
        required: false
        default: https://app.devguard.org
      devguard-artifact-name:
        description: The name of the artifact (in purl format e.g., pkg:oci/k8s-tools)
        type: string
        required: false
        default: ""
      devguard-origin:
        description: "Origin of the resource/information (how it was generated). Examples: 'source-scanning', 'container-scanning', 'base-image'. (default 'DEFAULT')"
        type: string
        required: false
        default: DEFAULT
      image-tar-path:
        description: The path to the local image tar file to scan (e.g. image.tar). Used when image_tag is empty.
        type: string
        required: false
        default: image.tar
      fetch-image-from-registry:
        description: If the artifact size is too big for your GitLab instance and the image was pushed directly to the registry during build (small_artifact_registry=true), set this to true to pull the image from the registry for scanning instead of using a local tar artifact.
        type: string
        required: false
        default: "false"
      default-ref:
        description: Default branch reference
        type: string
        required: false
        default: ${{ github.event.repository.default_branch }}
      fail-on-risk:
        description: "The risk level to fail the job on. Options are: none, low, medium, high, critical"
        type: string
        required: false
        default: critical
      fail-on-cvss:
        description: "The CVSS score to fail the job on. Options are: none, low, medium, high, critical"
        type: string
        required: false
        default: critical
      ignore-external-references:
        description: Whether to ignore external references in any discovered attestations. This is useful when you are using this component to scan your own built images which have a valid vex attestation.
        type: string
        required: false
        default: "false"
      ignore-upstream-attestations:
        description: Whether to ignore upstream attestations when scanning the image.
        type: string
        required: false
        default: "false"
      should-deploy:
        description: Should the attestation job run
        type: boolean
        required: false
        default: true
      image-already-in-registry:
        description: If set to true, the image wont be pushed again
        type: boolean
        required: false
        default: false
      artifact-suffix:
        description: Suffix used to look up build artifacts by name. Defaults to image-suffix when not set.
        type: string
        required: false
        default: ""
      supply-chain-id:
        description: The supply chain ID to use for the in-toto attestation
        type: string
        required: false
        default: ${{ github.sha }}
      upstream-version:
        description: Upstream version to use for tag generation. If not provided, the job will ignore this parameter.
        type: string
        required: false
        default: "0"
      create-root-manifest:
        description: Whether to also create a manifest without the branch ref in the tag.
        type: string
        required: false
        default: ${{ github.ref_type == 'tag' }}
      registry:
        description: Container registry URL
        type: string
        required: false
        default: ghcr.io
      registry-user:
        description: Container registry username
        type: string
        required: false
        default: ${{ github.actor }}
      nix-target-amd64:
        description: Nix flake build target for amd64 (e.g. devguard-0-amd64)
        type: string
        required: false
      nix-target-arm64:
        description: Nix flake build target for arm64 (e.g. devguard-0-arm64)
        type: string
        required: false
      runner-amd64:
        description: GitHub Actions runner label for the amd64 build
        type: string
        required: false
        default: ubuntu-latest
      runner-arm64:
        description: GitHub Actions runner label for the arm64 build
        type: string
        required: false
        default: ubuntu-24.04-arm
      artifact-name-suffix:
        description: Suffix appended to artifact names to avoid conflicts when building multiple images
        type: string
        required: false
        default: ""
    secrets:
      devguard-token:
        description: DevGuard API token
        required: true
      nix-cache-secret-key:
        description: Nix binary cache signing secret key.
        required: false
      nix-cache-aws-access-key-id:
        description: AWS access key ID for the Nix S3 cache.
        required: false
      nix-cache-aws-secret-access-key:
        description: AWS secret access key for the Nix S3 cache.
        required: false
      registry-password:
        description: Registry password for pulling the image.
        required: false
jobs:
  devguard_build_nix_amd64:
    runs-on: ${{ inputs.runner-amd64 }}
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install Nix
        uses: cachix/install-nix-action@8aa03977d8d733052d78f4e008a241fd1dbf36b3
        with:
          install_url: ${{ format('https://releases.nixos.org/nix/nix-{0}/install', inputs.nix-version) }}
          extra_nix_config: |-
            experimental-features = nix-command flakes
            ${{ inputs.nix-cache-substituter != '' && format('substituters = https://cache.nixos.org {0}', inputs.nix-cache-substituter) || '' }}
            ${{ inputs.nix-cache-public-key != '' && format('trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= {0}', inputs.nix-cache-public-key) || '' }}
      - name: Install crane and devguard-scanner
        run: nix profile install nixpkgs#crane github:l3montree-dev/devguard#devguardScanner
      - name: In-Toto Provenance record start
        env:
          DEVGUARD_API_URL: ${{ inputs.devguard-api-url }}
          DEVGUARD_ASSET_NAME: ${{ inputs.devguard-asset-name }}
        run: devguard-scanner intoto start --step=build --token=${{ secrets.devguard-token }} --apiUrl=$DEVGUARD_API_URL --assetName=$DEVGUARD_ASSET_NAME --supplyChainId=$GITHUB_SHA
        continue-on-error: true
      - name: Write Nix cache secret key
        if: inputs.nix-cache-s3-endpoint != ''
        run: echo "${{ secrets.nix-cache-secret-key }}" > /tmp/nix-cache-priv-key.pem
      - name: Build OCI image with Nix
        env:
          NIX_TARGET: ${{ inputs.nix-target-amd64 }}
          NIX_IMPURE: ${{ inputs.nix-impure }}
        run: nix build .#$NIX_TARGET $([ "$NIX_IMPURE" = "true" ] && echo --impure)
      - name: Push build results to Nix cache
        if: inputs.nix-cache-s3-endpoint != ''
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.nix-cache-aws-access-key-id }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.nix-cache-aws-secret-access-key }}
          NIX_CACHE_S3_BUCKET: ${{ inputs.nix-cache-s3-bucket }}
          NIX_CACHE_S3_ENDPOINT: ${{ inputs.nix-cache-s3-endpoint }}
          NIX_CACHE_REGION: ${{ inputs.nix-cache-region }}
        run: |-
          mkdir -p ~/.aws
          echo "[profile nix-cache]" >> ~/.aws/config
          echo "s3.addressing_style = path" >> ~/.aws/config
          nix copy $(nix-store -qR $(readlink result)) \
            --to "s3://$NIX_CACHE_S3_BUCKET?endpoint=$NIX_CACHE_S3_ENDPOINT&region=$NIX_CACHE_REGION&scheme=https&profile=nix-cache&secret-key=/tmp/nix-cache-priv-key.pem" || true
      - name: Prepare image.tar
        run: gunzip -c "$(readlink -f result)" > image.tar
      - name: Get image digest
        run: crane digest --tarball=image.tar > image-digest.txt
      - name: Upload oci-image artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: oci-image${{ inputs.artifact-name-suffix }}-amd64
          path: image.tar
      - name: Set image tag
        id: set-image-tag
        env:
          IMAGE_NAME: ${{ inputs.image-name }}
          ARCHITECTURE: amd64
          IS_TAG: ${{ inputs.is-tag }}
        run: |-
          devguard-scanner generate-tag \
            --imagePath="$IMAGE_NAME" \
            --ref="$GITHUB_REF_NAME" \
            --isTag=$IS_TAG \
            --architecture="$ARCHITECTURE" \
            >> image-tag-env.txt
          IMAGE_TAG=$(grep '^IMAGE_TAG=' image-tag-env.txt | cut -d= -f2-)
          ARTIFACT_NAME=$(grep '^ARTIFACT_NAME=' image-tag-env.txt | cut -d= -f2-)
          ARTIFACT_URL_ENCODED=$(grep '^ARTIFACT_URL_ENCODED=' image-tag-env.txt | cut -d= -f2-)
          echo "$IMAGE_TAG" > image-tag.txt
          echo "$ARTIFACT_NAME" > artifact-purl.txt
          echo "$ARTIFACT_URL_ENCODED" > artifact-purl-safe.txt
          echo "IMAGE_TAG=$IMAGE_TAG" >> "$GITHUB_ENV"
          echo "ARTIFACT_NAME=$ARTIFACT_NAME" >> "$GITHUB_ENV"
      - name: Upload image-tag artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: image-tag${{ inputs.artifact-name-suffix }}-amd64
          path: image-tag.txt
      - name: Upload image-digest artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: image-digest${{ inputs.artifact-name-suffix }}-amd64
          path: image-digest.txt
      - name: Upload artifact-purl artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: artifact-purl${{ inputs.artifact-name-suffix }}-amd64
          path: artifact-purl.txt
      - name: Upload artifact-purl-safe artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: artifact-purl-safe${{ inputs.artifact-name-suffix }}-amd64
          path: artifact-purl-safe.txt
      - name: In-Toto Provenance record stop
        env:
          DEVGUARD_API_URL: ${{ inputs.devguard-api-url }}
          DEVGUARD_ASSET_NAME: ${{ inputs.devguard-asset-name }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          IS_TAG: ${{ inputs.is-tag }}
          COMMIT_REF: ${{ inputs.commit-ref }}
        run: devguard-scanner intoto stop --step=build --products=image-digest.txt --products=image-tag.txt --token=${{ secrets.devguard-token }} --apiUrl=$DEVGUARD_API_URL --assetName=$DEVGUARD_ASSET_NAME --supplyChainId=$GITHUB_SHA --generateSlsaProvenance --defaultRef=$DEFAULT_BRANCH --isTag=$IS_TAG --ref=$COMMIT_REF
        continue-on-error: true
      - name: Upload SLSA Provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          path: build.provenance.json
          name: build${{ inputs.artifact-name-suffix }}-amd64.provenance.json
    name: devguard:build-nix-amd64
  devguard_build_nix_arm64:
    runs-on: ${{ inputs.runner-arm64 }}
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Install Nix
        uses: cachix/install-nix-action@8aa03977d8d733052d78f4e008a241fd1dbf36b3
        with:
          install_url: ${{ format('https://releases.nixos.org/nix/nix-{0}/install', inputs.nix-version) }}
          extra_nix_config: |-
            experimental-features = nix-command flakes
            ${{ inputs.nix-cache-substituter != '' && format('substituters = https://cache.nixos.org {0}', inputs.nix-cache-substituter) || '' }}
            ${{ inputs.nix-cache-public-key != '' && format('trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= {0}', inputs.nix-cache-public-key) || '' }}
      - name: Install crane and devguard-scanner
        run: nix profile install nixpkgs#crane github:l3montree-dev/devguard#devguardScanner
      - name: In-Toto Provenance record start
        env:
          DEVGUARD_API_URL: ${{ inputs.devguard-api-url }}
          DEVGUARD_ASSET_NAME: ${{ inputs.devguard-asset-name }}
        run: devguard-scanner intoto start --step=build --token=${{ secrets.devguard-token }} --apiUrl=$DEVGUARD_API_URL --assetName=$DEVGUARD_ASSET_NAME --supplyChainId=$GITHUB_SHA
        continue-on-error: true
      - name: Write Nix cache secret key
        if: inputs.nix-cache-s3-endpoint != ''
        run: echo "${{ secrets.nix-cache-secret-key }}" > /tmp/nix-cache-priv-key.pem
      - name: Build OCI image with Nix
        env:
          NIX_TARGET: ${{ inputs.nix-target-arm64 }}
          NIX_IMPURE: ${{ inputs.nix-impure }}
        run: nix build .#$NIX_TARGET $([ "$NIX_IMPURE" = "true" ] && echo --impure)
      - name: Push build results to Nix cache
        if: inputs.nix-cache-s3-endpoint != ''
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.nix-cache-aws-access-key-id }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.nix-cache-aws-secret-access-key }}
          NIX_CACHE_S3_BUCKET: ${{ inputs.nix-cache-s3-bucket }}
          NIX_CACHE_S3_ENDPOINT: ${{ inputs.nix-cache-s3-endpoint }}
          NIX_CACHE_REGION: ${{ inputs.nix-cache-region }}
        run: |-
          mkdir -p ~/.aws
          echo "[profile nix-cache]" >> ~/.aws/config
          echo "s3.addressing_style = path" >> ~/.aws/config
          nix copy $(nix-store -qR $(readlink result)) \
            --to "s3://$NIX_CACHE_S3_BUCKET?endpoint=$NIX_CACHE_S3_ENDPOINT&region=$NIX_CACHE_REGION&scheme=https&profile=nix-cache&secret-key=/tmp/nix-cache-priv-key.pem" || true
      - name: Prepare image.tar
        run: gunzip -c "$(readlink -f result)" > image.tar
      - name: Get image digest
        run: crane digest --tarball=image.tar > image-digest.txt
      - name: Upload oci-image artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: oci-image${{ inputs.artifact-name-suffix }}-arm64
          path: image.tar
      - name: Set image tag
        id: set-image-tag
        env:
          IMAGE_NAME: ${{ inputs.image-name }}
          ARCHITECTURE: arm64
          IS_TAG: ${{ inputs.is-tag }}
        run: |-
          devguard-scanner generate-tag \
            --imagePath="$IMAGE_NAME" \
            --ref="$GITHUB_REF_NAME" \
            --isTag=$IS_TAG \
            --architecture="$ARCHITECTURE" \
            >> image-tag-env.txt
          IMAGE_TAG=$(grep '^IMAGE_TAG=' image-tag-env.txt | cut -d= -f2-)
          ARTIFACT_NAME=$(grep '^ARTIFACT_NAME=' image-tag-env.txt | cut -d= -f2-)
          ARTIFACT_URL_ENCODED=$(grep '^ARTIFACT_URL_ENCODED=' image-tag-env.txt | cut -d= -f2-)
          echo "$IMAGE_TAG" > image-tag.txt
          echo "$ARTIFACT_NAME" > artifact-purl.txt
          echo "$ARTIFACT_URL_ENCODED" > artifact-purl-safe.txt
          echo "IMAGE_TAG=$IMAGE_TAG" >> "$GITHUB_ENV"
          echo "ARTIFACT_NAME=$ARTIFACT_NAME" >> "$GITHUB_ENV"
      - name: Upload image-tag artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: image-tag${{ inputs.artifact-name-suffix }}-arm64
          path: image-tag.txt
      - name: Upload image-digest artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: image-digest${{ inputs.artifact-name-suffix }}-arm64
          path: image-digest.txt
      - name: Upload artifact-purl artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: artifact-purl${{ inputs.artifact-name-suffix }}-arm64
          path: artifact-purl.txt
      - name: Upload artifact-purl-safe artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: artifact-purl-safe${{ inputs.artifact-name-suffix }}-arm64
          path: artifact-purl-safe.txt
      - name: In-Toto Provenance record stop
        env:
          DEVGUARD_API_URL: ${{ inputs.devguard-api-url }}
          DEVGUARD_ASSET_NAME: ${{ inputs.devguard-asset-name }}
          DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
          IS_TAG: ${{ inputs.is-tag }}
          COMMIT_REF: ${{ inputs.commit-ref }}
        run: devguard-scanner intoto stop --step=build --products=image-digest.txt --products=image-tag.txt --token=${{ secrets.devguard-token }} --apiUrl=$DEVGUARD_API_URL --assetName=$DEVGUARD_ASSET_NAME --supplyChainId=$GITHUB_SHA --generateSlsaProvenance --defaultRef=$DEFAULT_BRANCH --isTag=$IS_TAG --ref=$COMMIT_REF
        continue-on-error: true
      - name: Upload SLSA Provenance
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          path: build.provenance.json
          name: build${{ inputs.artifact-name-suffix }}-arm64.provenance.json
    name: devguard:build-nix-arm64
  devguard_container_scanning_amd64:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          submodules: recursive
          fetch-depth: 0
          persist-credentials: true
      - name: Download Docker image artifact (created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: oci-image${{ inputs.artifact-name-suffix }}-amd64
          path: .
        if: ${{ inputs.fetch-image-from-registry == 'false' }}
      - name: Download image-tag artifact (created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-tag${{ inputs.artifact-name-suffix }}-amd64
          path: .
      - name: Download artifact purl (created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: artifact-purl${{ inputs.artifact-name-suffix }}-amd64
        if: ${{ inputs.devguard-artifact-name == '' }}
      - name: Resolve artifact name
        env:
          DEVGUARD_ARTIFACT_NAME: ${{ inputs.devguard-artifact-name }}
        run: |-
          if [ -z "$DEVGUARD_ARTIFACT_NAME" ] && [ -f artifact-purl.txt ]; then
            echo "ARTIFACT_NAME=$(cat artifact-purl.txt)" >> $GITHUB_ENV
          else
            echo "ARTIFACT_NAME=$DEVGUARD_ARTIFACT_NAME" >> $GITHUB_ENV
          fi
      - name: Setup crane
        uses: imjasonh/setup-crane@cacf28038427f6fb033ef9022f6a2281f23cb3f2
      - name: Download OCI Image from registry
        run: crane pull $(cat image-tag.txt) image.tar
        if: ${{ inputs.fetch-image-from-registry == 'true' }}
      - name: DevGuard Container-Scanning
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        continue-on-error: ${{ inputs.allow-failure }}
        with:
          args: devguard-scanner container-scanning --origin="${{ inputs.devguard-origin }}" --assetName="${{ inputs.devguard-asset-name }}" --apiUrl="${{ inputs.devguard-api-url }}" --token="${{ secrets.devguard-token }}" --path="${{ inputs.image-tar-path }}" --defaultRef="${{ inputs.default-ref }}" --ref="${{ inputs.commit-ref }}" --isTag="${{ inputs.is-tag }}" --artifactName="${{ env.ARTIFACT_NAME }}" --webUI="${{ inputs.devguard-web-ui }}" --failOnRisk="${{ inputs.fail-on-risk }}" --failOnCVSS="${{ inputs.fail-on-cvss }}" --ignoreExternalReferences=${{ inputs.ignore-external-references }} --ignoreUpstreamAttestations=${{ inputs.ignore-upstream-attestations }}
        env:
          ARTIFACT_NAME: ${{ env.ARTIFACT_NAME }}
    needs:
      - devguard_build_nix_amd64
    name: devguard:container-scanning-amd64
  devguard_container_scanning_arm64:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          submodules: recursive
          fetch-depth: 0
          persist-credentials: true
      - name: Download Docker image artifact (created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: oci-image${{ inputs.artifact-name-suffix }}-arm64
          path: .
        if: ${{ inputs.fetch-image-from-registry == 'false' }}
      - name: Download image-tag artifact (created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-tag${{ inputs.artifact-name-suffix }}-arm64
          path: .
      - name: Download artifact purl (created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: artifact-purl${{ inputs.artifact-name-suffix }}-arm64
        if: ${{ inputs.devguard-artifact-name == '' }}
      - name: Resolve artifact name
        env:
          DEVGUARD_ARTIFACT_NAME: ${{ inputs.devguard-artifact-name }}
        run: |-
          if [ -z "$DEVGUARD_ARTIFACT_NAME" ] && [ -f artifact-purl.txt ]; then
            echo "ARTIFACT_NAME=$(cat artifact-purl.txt)" >> $GITHUB_ENV
          else
            echo "ARTIFACT_NAME=$DEVGUARD_ARTIFACT_NAME" >> $GITHUB_ENV
          fi
      - name: Setup crane
        uses: imjasonh/setup-crane@cacf28038427f6fb033ef9022f6a2281f23cb3f2
      - name: Download OCI Image from registry
        run: crane pull $(cat image-tag.txt) image.tar
        if: ${{ inputs.fetch-image-from-registry == 'true' }}
      - name: DevGuard Container-Scanning
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        continue-on-error: ${{ inputs.allow-failure }}
        with:
          args: devguard-scanner container-scanning --origin="${{ inputs.devguard-origin }}" --assetName="${{ inputs.devguard-asset-name }}" --apiUrl="${{ inputs.devguard-api-url }}" --token="${{ secrets.devguard-token }}" --path="${{ inputs.image-tar-path }}" --defaultRef="${{ inputs.default-ref }}" --ref="${{ inputs.commit-ref }}" --isTag="${{ inputs.is-tag }}" --artifactName="${{ env.ARTIFACT_NAME }}" --webUI="${{ inputs.devguard-web-ui }}" --failOnRisk="${{ inputs.fail-on-risk }}" --failOnCVSS="${{ inputs.fail-on-cvss }}" --ignoreExternalReferences=${{ inputs.ignore-external-references }} --ignoreUpstreamAttestations=${{ inputs.ignore-upstream-attestations }}
        env:
          ARTIFACT_NAME: ${{ env.ARTIFACT_NAME }}
    needs:
      - devguard_build_nix_arm64
    name: devguard:container-scanning-arm64
  devguard_deploy_amd64:
    runs-on: ubuntu-latest
    if: inputs.should-deploy
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          submodules: recursive
          fetch-depth: 0
          persist-credentials: true
      - name: Download oci-image artifact (can be created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: oci-image${{ inputs.artifact-name-suffix }}-amd64
          path: .
        if: inputs.image-already-in-registry == false
      - name: Download image-tag artifact (can be created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-tag${{ inputs.artifact-name-suffix }}-amd64
          path: .
      - name: Download image-digest artifact (can be created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-digest${{ inputs.artifact-name-suffix }}-amd64
          path: .
      - name: Read image-digest.txt
        id: read-digest
        run: echo "DIGEST=$(cat image-digest.txt)" >> $GITHUB_ENV
      - name: In-Toto Provenance run
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        with:
          args: devguard-scanner intoto run --step=deploy --materials=image-tag.txt --products=image-tag.txt --products=image-digest.txt --token=${{ secrets.devguard-token }} --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --supplyChainId=${{ inputs.supply-chain-id }} --supplyChainOutputDigest="${{ env.DIGEST }}" --defaultRef=${{ inputs.default-ref }} --isTag=${{ inputs.is-tag }} --ref=${{ inputs.commit-ref }}
        continue-on-error: true
      - name: Setup crane
        uses: imjasonh/setup-crane@cacf28038427f6fb033ef9022f6a2281f23cb3f2
      - name: Push oci image to GitHub image Registry
        run: crane push image.tar "$(cat image-tag.txt)"
        if: inputs.image-already-in-registry == false
    needs:
      - devguard_build_nix_amd64
      - devguard_container_scanning_amd64
    name: devguard:deploy-amd64
  devguard_deploy_arm64:
    runs-on: ubuntu-latest
    if: inputs.should-deploy
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          submodules: recursive
          fetch-depth: 0
          persist-credentials: true
      - name: Download oci-image artifact (can be created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: oci-image${{ inputs.artifact-name-suffix }}-arm64
          path: .
        if: inputs.image-already-in-registry == false
      - name: Download image-tag artifact (can be created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-tag${{ inputs.artifact-name-suffix }}-arm64
          path: .
      - name: Download image-digest artifact (can be created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-digest${{ inputs.artifact-name-suffix }}-arm64
          path: .
      - name: Read image-digest.txt
        id: read-digest
        run: echo "DIGEST=$(cat image-digest.txt)" >> $GITHUB_ENV
      - name: In-Toto Provenance run
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        with:
          args: devguard-scanner intoto run --step=deploy --materials=image-tag.txt --products=image-tag.txt --products=image-digest.txt --token=${{ secrets.devguard-token }} --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --supplyChainId=${{ inputs.supply-chain-id }} --supplyChainOutputDigest="${{ env.DIGEST }}" --defaultRef=${{ inputs.default-ref }} --isTag=${{ inputs.is-tag }} --ref=${{ inputs.commit-ref }}
        continue-on-error: true
      - name: Setup crane
        uses: imjasonh/setup-crane@cacf28038427f6fb033ef9022f6a2281f23cb3f2
      - name: Push oci image to GitHub image Registry
        run: crane push image.tar "$(cat image-tag.txt)"
        if: inputs.image-already-in-registry == false
    needs:
      - devguard_build_nix_arm64
      - devguard_container_scanning_arm64
    name: devguard:deploy-arm64
  devguard_create_manifest_multi_arch:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - name: Resolve registry password
        run: |-
          if [ -n "$SECRET_PASSWORD" ]; then
            echo "REGISTRY_PASSWORD=$SECRET_PASSWORD" >> $GITHUB_ENV
          else
            echo "REGISTRY_PASSWORD=${{ github.token }}" >> $GITHUB_ENV
          fi
        env:
          SECRET_PASSWORD: ${{ secrets.registry-password }}
      - name: Download amd64 image-tag
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-tag${{ inputs.artifact-name-suffix }}-amd64
          path: amd64
      - name: Download arm64 image-tag
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-tag${{ inputs.artifact-name-suffix }}-arm64
          path: arm64
      - name: Log in to registry
        uses: docker/login-action@3864d6aed8ff134b2ed894ce00c87695c709c870
        with:
          registry: ${{ inputs.registry }}
          username: ${{ inputs.registry-user }}
          password: ${{ env.REGISTRY_PASSWORD }}
      - name: Create and push multi-arch manifest
        env:
          CREATE_ROOT_MANIFEST: ${{ inputs.create-root-manifest }}
        run: |-
          AMD64_TAG=$(cat amd64/image-tag.txt)
          ARM64_TAG=$(cat arm64/image-tag.txt)

          if [ -z "$AMD64_TAG" ] || [ -z "$ARM64_TAG" ]; then
            echo "ERROR: Could not read arch-specific IMAGE_TAG from image-tag artifacts"
            exit 1
          fi

          echo "amd64: $AMD64_TAG"
          echo "arm64: $ARM64_TAG"

          BASE_TAG="${AMD64_TAG%-amd64}"

          echo "Creating manifest: $BASE_TAG -> $AMD64_TAG + $ARM64_TAG"
          docker manifest create "$BASE_TAG" "$AMD64_TAG" "$ARM64_TAG"
          docker manifest push "$BASE_TAG"

          if [ "$CREATE_ROOT_MANIFEST" = "true" ]; then
            ROOT_TAG=$(echo "$BASE_TAG" | sed "s/-${GITHUB_REF_NAME}//")
            if [ "$ROOT_TAG" != "$BASE_TAG" ]; then
              echo "Creating root manifest: $ROOT_TAG"
              docker manifest create "$ROOT_TAG" "$AMD64_TAG" "$ARM64_TAG"
              docker manifest push "$ROOT_TAG"
            fi
          fi
    needs:
      - devguard_deploy_amd64
      - devguard_deploy_arm64
    name: devguard:create-manifest-multi-arch
  devguard_sign_amd64:
    runs-on: ubuntu-latest
    if: inputs.should-deploy
    steps:
      - name: Resolve registry password
        run: |-
          if [ -n "$SECRET_PASSWORD" ]; then
            echo "REGISTRY_PASSWORD=$SECRET_PASSWORD" >> $GITHUB_ENV
          else
            echo "REGISTRY_PASSWORD=${{ github.token }}" >> $GITHUB_ENV
          fi
        env:
          SECRET_PASSWORD: ${{ secrets.registry-password }}
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          submodules: recursive
          fetch-depth: 0
          persist-credentials: true
      - name: Download image-tag artifact
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-tag${{ inputs.artifact-name-suffix }}-amd64
          path: .
      - name: Download image-digest artifact
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-digest${{ inputs.artifact-name-suffix }}-amd64
          path: .
      - name: Set Image to be signed
        run: echo "IMAGE_TAG_AND_DIGEST=$(cat image-tag.txt)@$(cat image-digest.txt)" >> $GITHUB_ENV
      - name: DevGuard Image-Signing
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        with:
          args: devguard-scanner sign -u ${{ inputs.registry-user }} -r ${{ inputs.registry }} -p "${{ env.REGISTRY_PASSWORD }}" --token="${{ secrets.devguard-token }}" ${{ env.IMAGE_TAG_AND_DIGEST }} --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }}
    needs:
      - devguard_build_nix_amd64
      - devguard_create_manifest_multi_arch
    name: devguard:sign-amd64
  devguard_sign_arm64:
    runs-on: ubuntu-latest
    if: inputs.should-deploy
    steps:
      - name: Resolve registry password
        run: |-
          if [ -n "$SECRET_PASSWORD" ]; then
            echo "REGISTRY_PASSWORD=$SECRET_PASSWORD" >> $GITHUB_ENV
          else
            echo "REGISTRY_PASSWORD=${{ github.token }}" >> $GITHUB_ENV
          fi
        env:
          SECRET_PASSWORD: ${{ secrets.registry-password }}
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          submodules: recursive
          fetch-depth: 0
          persist-credentials: true
      - name: Download image-tag artifact
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-tag${{ inputs.artifact-name-suffix }}-arm64
          path: .
      - name: Download image-digest artifact
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-digest${{ inputs.artifact-name-suffix }}-arm64
          path: .
      - name: Set Image to be signed
        run: echo "IMAGE_TAG_AND_DIGEST=$(cat image-tag.txt)@$(cat image-digest.txt)" >> $GITHUB_ENV
      - name: DevGuard Image-Signing
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        with:
          args: devguard-scanner sign -u ${{ inputs.registry-user }} -r ${{ inputs.registry }} -p "${{ env.REGISTRY_PASSWORD }}" --token="${{ secrets.devguard-token }}" ${{ env.IMAGE_TAG_AND_DIGEST }} --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }}
    needs:
      - devguard_build_nix_arm64
      - devguard_create_manifest_multi_arch
    name: devguard:sign-arm64
  devguard_attest_amd64:
    runs-on: ubuntu-latest
    if: inputs.should-deploy
    steps:
      - name: Resolve registry password
        run: |-
          if [ -n "$SECRET_PASSWORD" ]; then
            echo "REGISTRY_PASSWORD=$SECRET_PASSWORD" >> $GITHUB_ENV
          else
            echo "REGISTRY_PASSWORD=${{ github.token }}" >> $GITHUB_ENV
          fi
        env:
          SECRET_PASSWORD: ${{ secrets.registry-password }}
      - name: Download image-digest artifact
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-digest${{ inputs.artifact-name-suffix }}-amd64
          path: .
        continue-on-error: true
      - name: Download image-tag artifact
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-tag${{ inputs.artifact-name-suffix }}-amd64
          path: .
      - name: Download artifact purl (can be created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: artifact-purl${{ inputs.artifact-name-suffix }}-amd64
        if: inputs.devguard-artifact-name == ''
      - name: Download safe-artifact (can be created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: artifact-purl-safe${{ inputs.artifact-name-suffix }}-amd64
        if: inputs.devguard-artifact-name == ''
      - name: set artifact-name variable if it is empty
        env:
          DEVGUARD_ARTIFACT_NAME: ${{ inputs.devguard-artifact-name }}
        run: |-
          if [ -z "$DEVGUARD_ARTIFACT_NAME" ] && [ -f artifact-purl.txt ]; then
            echo "ARTIFACT_NAME=$(cat artifact-purl.txt)" >> $GITHUB_ENV
            echo "Using artifact name from file: $ARTIFACT_NAME"
            # For API calls, use safe artifact name if it exists
            if [ -f artifact-purl-safe.txt ]; then
              echo "API_ARTIFACT_NAME=$(cat artifact-purl-safe.txt)" >> $GITHUB_ENV
            else
              echo "API_ARTIFACT_NAME=$(cat artifact-purl.txt)" >> $GITHUB_ENV
            fi
          else
            # make sure to url encode
            echo "ARTIFACT_NAME=$DEVGUARD_ARTIFACT_NAME" >> $GITHUB_ENV
            echo "API_ARTIFACT_NAME=$(echo -n "$DEVGUARD_ARTIFACT_NAME" | jq -s -R -r @uri)" >> $GITHUB_ENV
            echo "Using provided artifact name: $DEVGUARD_ARTIFACT_NAME"
            echo "Encoded: $API_ARTIFACT_NAME"
          fi
          echo "Resolved artifact name for attestation: $DEVGUARD_ARTIFACT_NAME"
      - name: Get and Attest SBOM
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        with:
          args: |-
            sh -c "
              slug=$(devguard-scanner slug ${{ inputs.commit-ref }}) &&
              artifact_name="$ARTIFACT_NAME" &&
              echo 'Fetching SBOM for artifact:' '${{ env.API_ARTIFACT_NAME }}' &&
              devguard-scanner curl '${{ inputs.devguard-api-url }}/api/v1/organizations/${{ inputs.devguard-asset-name }}/refs/'$slug'/artifacts/${{ env.API_ARTIFACT_NAME }}/sbom.json/' --token='${{ secrets.devguard-token }}' > /tmp/sbom.json &&
              echo 'SBOM downloaded to /tmp/sbom.json' &&
              if [ -f image-digest.txt ]; then
                echo 'Attesting SBOM with image digest present' &&
                devguard-scanner attest -u ${{ inputs.registry-user }} -r ${{ inputs.registry }} -p "${{ env.REGISTRY_PASSWORD }}" /tmp/sbom.json --predicateType='https://cyclonedx.org/bom' \"$(cat image-tag.txt)@$(cat image-digest.txt)\" --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              else
                echo 'Attesting SBOM without image digest' &&
                devguard-scanner attest /tmp/sbom.json --predicateType='https://cyclonedx.org/bom' --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              fi
            "
        env:
          API_ARTIFACT_NAME: ${{ env.API_ARTIFACT_NAME }}
          ARTIFACT_NAME: ${{ env.ARTIFACT_NAME }}
      - name: Get and Attest VeX
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        with:
          args: |-
            sh -c "
              slug=$(devguard-scanner slug ${{ inputs.commit-ref }}) &&
              artifact_name="$ARTIFACT_NAME" &&
              echo 'Fetching VeX for artifact:' '${{ env.API_ARTIFACT_NAME }}' &&
              devguard-scanner curl '${{ inputs.devguard-api-url }}/api/v1/organizations/${{ inputs.devguard-asset-name }}/refs/'$slug'/artifacts/${{ env.API_ARTIFACT_NAME }}/vex.json/' --token='${{ secrets.devguard-token }}' > /tmp/vex.json &&
              echo 'VeX downloaded to /tmp/vex.json' &&
              if [ -f image-digest.txt ]; then
                echo 'Attesting VeX with image digest present' &&
                devguard-scanner attest -u ${{ inputs.registry-user }} -r ${{ inputs.registry }} -p "${{ env.REGISTRY_PASSWORD }}" /tmp/vex.json \"$(cat image-tag.txt)@$(cat image-digest.txt)\" --token='${{ secrets.devguard-token }}' --predicateType='https://cyclonedx.org/vex' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              else
                echo 'Attesting VeX without image digest' &&
                devguard-scanner attest /tmp/vex.json --predicateType='https://cyclonedx.org/vex' --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              fi
            "
        env:
          API_ARTIFACT_NAME: ${{ env.API_ARTIFACT_NAME }}
          ARTIFACT_NAME: ${{ env.ARTIFACT_NAME }}
      - name: Get and Attest SAST-Results
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        with:
          args: |-
            sh -c "
              slug=$(devguard-scanner slug ${{ inputs.commit-ref }}) &&
              artifact_name="$ARTIFACT_NAME" &&
              echo 'Fetching SAST results for artifact:' '${{ env.ARTIFACT_NAME }}' &&
              devguard-scanner curl '${{ inputs.devguard-api-url }}/api/v1/organizations/${{ inputs.devguard-asset-name }}/refs/'$slug'/sarif.json' --token='${{ secrets.devguard-token }}' > /tmp/sarif.json &&
              echo 'SAST results downloaded to /tmp/sarif.json' &&
              if [ -f image-digest.txt ]; then
                echo 'Attesting SAST results with image digest present' &&
                devguard-scanner attest -u ${{ inputs.registry-user }} -r ghcr.io -p ${{ secrets.GITHUB_TOKEN }} /tmp/sarif.json \"$(cat image-tag.txt)@$(cat image-digest.txt)\" --predicateType='https://www.schemastore.org/schemas/json/sarif-2.1.0.json' --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              else
                echo 'Attesting SAST results without image digest' &&
                devguard-scanner attest /tmp/sarif.json --predicateType='https://www.schemastore.org/schemas/json/sarif-2.1.0.json' --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              fi
            "
        env:
          ARTIFACT_NAME: ${{ env.ARTIFACT_NAME }}
      - name: Download and Attest build-provenance.json
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: build${{ inputs.artifact-name-suffix }}-amd64.provenance.json
      - name: Attest build-provenance.json
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        continue-on-error: true
        with:
          args: |-
            sh -c "
              artifact_name="$ARTIFACT_NAME" &&
              echo 'Building provenance attestation for artifact:' '${{ env.ARTIFACT_NAME }}' &&
              if [ -f image-digest.txt ]; then
                echo 'Attesting provenance with image digest present' &&
                devguard-scanner attest -u ${{ inputs.registry-user }} -r ghcr.io -p ${{ secrets.GITHUB_TOKEN }} build.provenance.json \"$(cat image-tag.txt)@$(cat image-digest.txt)\" --predicateType='https://slsa.dev/provenance/v1' --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              else
                echo 'Attesting provenance without image digest' &&
                devguard-scanner attest build.provenance.json --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --predicateType='https://slsa.dev/provenance/v1' --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              fi
            "
        env:
          ARTIFACT_NAME: ${{ env.ARTIFACT_NAME }}
    needs:
      - devguard_build_nix_amd64
      - devguard_create_manifest_multi_arch
    name: devguard:attest-amd64
  devguard_attest_arm64:
    runs-on: ubuntu-latest
    if: inputs.should-deploy
    steps:
      - name: Resolve registry password
        run: |-
          if [ -n "$SECRET_PASSWORD" ]; then
            echo "REGISTRY_PASSWORD=$SECRET_PASSWORD" >> $GITHUB_ENV
          else
            echo "REGISTRY_PASSWORD=${{ github.token }}" >> $GITHUB_ENV
          fi
        env:
          SECRET_PASSWORD: ${{ secrets.registry-password }}
      - name: Download image-digest artifact
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-digest${{ inputs.artifact-name-suffix }}-arm64
          path: .
        continue-on-error: true
      - name: Download image-tag artifact
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: image-tag${{ inputs.artifact-name-suffix }}-arm64
          path: .
      - name: Download artifact purl (can be created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: artifact-purl${{ inputs.artifact-name-suffix }}-arm64
        if: inputs.devguard-artifact-name == ''
      - name: Download safe-artifact (can be created by build-image)
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: artifact-purl-safe${{ inputs.artifact-name-suffix }}-arm64
        if: inputs.devguard-artifact-name == ''
      - name: set artifact-name variable if it is empty
        env:
          DEVGUARD_ARTIFACT_NAME: ${{ inputs.devguard-artifact-name }}
        run: |-
          if [ -z "$DEVGUARD_ARTIFACT_NAME" ] && [ -f artifact-purl.txt ]; then
            echo "ARTIFACT_NAME=$(cat artifact-purl.txt)" >> $GITHUB_ENV
            echo "Using artifact name from file: $ARTIFACT_NAME"
            # For API calls, use safe artifact name if it exists
            if [ -f artifact-purl-safe.txt ]; then
              echo "API_ARTIFACT_NAME=$(cat artifact-purl-safe.txt)" >> $GITHUB_ENV
            else
              echo "API_ARTIFACT_NAME=$(cat artifact-purl.txt)" >> $GITHUB_ENV
            fi
          else
            # make sure to url encode
            echo "ARTIFACT_NAME=$DEVGUARD_ARTIFACT_NAME" >> $GITHUB_ENV
            echo "API_ARTIFACT_NAME=$(echo -n "$DEVGUARD_ARTIFACT_NAME" | jq -s -R -r @uri)" >> $GITHUB_ENV
            echo "Using provided artifact name: $DEVGUARD_ARTIFACT_NAME"
            echo "Encoded: $API_ARTIFACT_NAME"
          fi
          echo "Resolved artifact name for attestation: $DEVGUARD_ARTIFACT_NAME"
      - name: Get and Attest SBOM
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        with:
          args: |-
            sh -c "
              slug=$(devguard-scanner slug ${{ inputs.commit-ref }}) &&
              artifact_name="$ARTIFACT_NAME" &&
              echo 'Fetching SBOM for artifact:' '${{ env.API_ARTIFACT_NAME }}' &&
              devguard-scanner curl '${{ inputs.devguard-api-url }}/api/v1/organizations/${{ inputs.devguard-asset-name }}/refs/'$slug'/artifacts/${{ env.API_ARTIFACT_NAME }}/sbom.json/' --token='${{ secrets.devguard-token }}' > /tmp/sbom.json &&
              echo 'SBOM downloaded to /tmp/sbom.json' &&
              if [ -f image-digest.txt ]; then
                echo 'Attesting SBOM with image digest present' &&
                devguard-scanner attest -u ${{ inputs.registry-user }} -r ${{ inputs.registry }} -p "${{ env.REGISTRY_PASSWORD }}" /tmp/sbom.json --predicateType='https://cyclonedx.org/bom' \"$(cat image-tag.txt)@$(cat image-digest.txt)\" --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              else
                echo 'Attesting SBOM without image digest' &&
                devguard-scanner attest /tmp/sbom.json --predicateType='https://cyclonedx.org/bom' --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              fi
            "
        env:
          API_ARTIFACT_NAME: ${{ env.API_ARTIFACT_NAME }}
          ARTIFACT_NAME: ${{ env.ARTIFACT_NAME }}
      - name: Get and Attest VeX
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        with:
          args: |-
            sh -c "
              slug=$(devguard-scanner slug ${{ inputs.commit-ref }}) &&
              artifact_name="$ARTIFACT_NAME" &&
              echo 'Fetching VeX for artifact:' '${{ env.API_ARTIFACT_NAME }}' &&
              devguard-scanner curl '${{ inputs.devguard-api-url }}/api/v1/organizations/${{ inputs.devguard-asset-name }}/refs/'$slug'/artifacts/${{ env.API_ARTIFACT_NAME }}/vex.json/' --token='${{ secrets.devguard-token }}' > /tmp/vex.json &&
              echo 'VeX downloaded to /tmp/vex.json' &&
              if [ -f image-digest.txt ]; then
                echo 'Attesting VeX with image digest present' &&
                devguard-scanner attest -u ${{ inputs.registry-user }} -r ${{ inputs.registry }} -p "${{ env.REGISTRY_PASSWORD }}" /tmp/vex.json \"$(cat image-tag.txt)@$(cat image-digest.txt)\" --token='${{ secrets.devguard-token }}' --predicateType='https://cyclonedx.org/vex' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              else
                echo 'Attesting VeX without image digest' &&
                devguard-scanner attest /tmp/vex.json --predicateType='https://cyclonedx.org/vex' --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              fi
            "
        env:
          API_ARTIFACT_NAME: ${{ env.API_ARTIFACT_NAME }}
          ARTIFACT_NAME: ${{ env.ARTIFACT_NAME }}
      - name: Get and Attest SAST-Results
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        with:
          args: |-
            sh -c "
              slug=$(devguard-scanner slug ${{ inputs.commit-ref }}) &&
              artifact_name="$ARTIFACT_NAME" &&
              echo 'Fetching SAST results for artifact:' '${{ env.ARTIFACT_NAME }}' &&
              devguard-scanner curl '${{ inputs.devguard-api-url }}/api/v1/organizations/${{ inputs.devguard-asset-name }}/refs/'$slug'/sarif.json' --token='${{ secrets.devguard-token }}' > /tmp/sarif.json &&
              echo 'SAST results downloaded to /tmp/sarif.json' &&
              if [ -f image-digest.txt ]; then
                echo 'Attesting SAST results with image digest present' &&
                devguard-scanner attest -u ${{ inputs.registry-user }} -r ghcr.io -p ${{ secrets.GITHUB_TOKEN }} /tmp/sarif.json \"$(cat image-tag.txt)@$(cat image-digest.txt)\" --predicateType='https://www.schemastore.org/schemas/json/sarif-2.1.0.json' --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              else
                echo 'Attesting SAST results without image digest' &&
                devguard-scanner attest /tmp/sarif.json --predicateType='https://www.schemastore.org/schemas/json/sarif-2.1.0.json' --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              fi
            "
        env:
          ARTIFACT_NAME: ${{ env.ARTIFACT_NAME }}
      - name: Download and Attest build-provenance.json
        uses: actions/download-artifact@484a0b528fb4d7bd804637ccb632e47a0e638317
        with:
          name: build${{ inputs.artifact-name-suffix }}-arm64.provenance.json
      - name: Attest build-provenance.json
        uses: docker://ghcr.io/l3montree-dev/devguard/scanner:main
        continue-on-error: true
        with:
          args: |-
            sh -c "
              artifact_name="$ARTIFACT_NAME" &&
              echo 'Building provenance attestation for artifact:' '${{ env.ARTIFACT_NAME }}' &&
              if [ -f image-digest.txt ]; then
                echo 'Attesting provenance with image digest present' &&
                devguard-scanner attest -u ${{ inputs.registry-user }} -r ghcr.io -p ${{ secrets.GITHUB_TOKEN }} build.provenance.json \"$(cat image-tag.txt)@$(cat image-digest.txt)\" --predicateType='https://slsa.dev/provenance/v1' --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              else
                echo 'Attesting provenance without image digest' &&
                devguard-scanner attest build.provenance.json --token='${{ secrets.devguard-token }}' --apiUrl=${{ inputs.devguard-api-url }} --predicateType='https://slsa.dev/provenance/v1' --assetName=${{ inputs.devguard-asset-name }} --ref=${{ inputs.commit-ref }} --isTag=${{ inputs.is-tag }} --artifactName="$artifact_name"
              fi
            "
        env:
          ARTIFACT_NAME: ${{ env.ARTIFACT_NAME }}
    needs:
      - devguard_build_nix_arm64
      - devguard_create_manifest_multi_arch
    name: devguard:attest-arm64
