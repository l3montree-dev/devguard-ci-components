include:
  ## Sign OCI Image Test
  - local: "tests/test_sign-oci-image.yml"
    rules:
      - if: '$CI_SERVER_FQDN == "gitlab.com"'
  ## Secret Scanning Test
  - local: "tests/test_secret-scanning.yml"
    rules:
      - if: '$CI_SERVER_FQDN == "gitlab.com"'
  - local: "tests/test_discover-baseimage-attestations.yml"
    rules:
      - if: '$CI_SERVER_FQDN == "gitlab.com"'
  ## Software Composition Analysis Test
  - local: "tests/test_software-composition-analysis.yml"
    rules:
      - if: '$CI_SERVER_FQDN == "gitlab.com"'
  ## SBOM Upload Test
  - local: "tests/test_sbom_upload.yml"
    rules:
      - if: '$CI_SERVER_FQDN == "gitlab.com"'
  ## Container Scanning Test
  - local: "tests/test_container-scanning.yml"
    rules:
      - if: '$CI_SERVER_FQDN == "gitlab.com"'
  ## Build OCI Image Test
  - local: "tests/test_build-oci-image.yml"
    rules:
      - if: '$CI_SERVER_FQDN == "gitlab.com"'
  ## Full Test
  - local: "tests/test_full.yml"
    rules:
      - if: '$CI_SERVER_FQDN == "gitlab.com"'
  ## Container Lifecycle Test
  - local: "tests/test_container-lifecycle.yml"
    rules:
      - if: '$CI_SERVER_FQDN == "gitlab.com"'
  ## Generate Tag Test
  - local: "tests/test_generate-tag.yml"
    rules:
      - if: '$CI_SERVER_FQDN == "gitlab.com"'
