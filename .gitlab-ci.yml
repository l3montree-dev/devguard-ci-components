stages:
- test
- tag
- build
- container-scanning

include:
  - component: $CI_SERVER_FQDN/$CI_PROJECT_PATH/devguard@$CI_COMMIT_SHA
    inputs:
      asset_name: "test"
      api_url: "test"
      token: "test"
