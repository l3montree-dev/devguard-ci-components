stages:
- test
- tag
- build
- container-scanning

include:
  - component: $CI_SERVER_FQDN/$CI_PROJECT_PATH/devguard@$CI_COMMIT_SHA
    inputs:
      asset_name: "$DEVGUARD_ASSET_NAME"
      api_url: "$DEVGUARD_API_URL"
      token: "$DEVGUARD_TOKEN"

