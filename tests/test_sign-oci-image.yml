include:
  #- remote: "https://gitlab.opencode.de/l3montree/devguard-ci-components/-/raw/v0.17.1/templates/full.yml"
  #  inputs:
  #    asset_name: "$DEVGUARD_ASSET_NAME"
  #    token: "$DEVGUARD_TOKEN"
  #    api_url: "$DEVGUARD_API_URL"
  #    web_ui: "$DEVGUARD_WEB_UI"

  #########
  ## component: sign-oci-image
  #########
  #### Test case: sign single image in projects own registry
  - component: $CI_SERVER_FQDN/l3montree/devguard/sign-oci-image@chore/generalization
    inputs:
      devguard_asset_name: "$DEVGUARD_ASSET_NAME"
      devguard_token: "$DEVGUARD_TOKEN"
      devguard_api_url: "$DEVGUARD_API_URL"
      images:
        - "$CI_REGISTRY_IMAGE/testing/signing:latest"
  #### Test case: sign single image in third party registry
  # ...
  #### Test case: sign multiple images in projects own registry
  # ...
  #### Test case: sign multiple images in third party registry