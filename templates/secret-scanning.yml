spec:
  inputs:
    scan_stage:
      default: 'test'
      description: 'The stage where the scan is run'

---
secret-scanning:
  image:
    name: ghcr.io/gitleaks/gitleaks:v8.19.3@sha256:b1081012aeb9026447deb2ecf4671f7a71cc035b9a1ce23a36c0a853c5dfde95
    entrypoint: [""]
  dependencies: []
  needs: []
  stage: $[[ inputs.scan_stage ]]
  script: gitleaks detect -b leaks-baseline.json -v
