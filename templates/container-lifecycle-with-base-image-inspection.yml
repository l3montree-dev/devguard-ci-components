# Copyright 2025 l3montree GmbH.
# SPDX-License-Identifier: AGPL-3.0
spec:
  inputs:
    # Stage Configuration
    build_stage:
      default: "build"
      description: "The stage where the build runs"
    scan_stage:
      default: "test"
      description: "The stage where scanning runs"
    deploy_stage:
      default: "deploy"
      description: "The stage where signing and attestation runs"

    # DevGuard Configuration
    devguard_token:
      description: "The token to authenticate against the DevGuard API"
    devguard_api_url:
      default: "https://api.devguard.org"
      description: "The DevGuard API URL"
    devguard_asset_name:
      description: "The name of the asset"
    devguard_artifact_name:
      default: ""
      description: "The name of the artifact you are building. Useful when a pipeline builds more than one artifact (e.g. different containers). If not provided, will use the generated PURL from the built image."
    devguard_web_ui:
      default: "https://app.devguard.org"
      description: "The DevGuard Web UI base URL"
    devguard_origin:
      default: "DEFAULT"
      description: "Origin string for DevGuard requests (e.g., 'container-scanning')."

    # Build Configuration
    build_args:
      default: "--context $CI_PROJECT_DIR --dockerfile $CI_PROJECT_DIR/Dockerfile"
      description: "The build arguments to pass to the Kaniko build command"
    push_image:
      default: "true"
      description: "Whether to push the image to the registry"
    image_suffix:
      default: "default"
      description: "Suffix for the image name (e.g. 'web'). Leave empty for no suffix. This will create images like $CONTAINER_IMAGE/<IMAGE_SUFFIX>:tag"
    image_tag:
      default: "$CI_REGISTRY_IMAGE:$CI_COMMIT_SHA"
      description: "Tag for the built image"

    # Job suffix used to disambiguate job names across multiple instances
    job_suffix:
      default: ""
      description: "Suffix for the job name. Provide when using the component multiple times in a pipeline."

    # Scanning Configuration
    fail_on_risk:
      default: "critical"
      description: "The risk level to fail the job on. Options are: none, low, medium, high, critical"
    fail_on_cvss:
      default: "critical"
      description: "The CVSS score to fail the job on. Options are: none, low, medium, high, critical"
    ignore_external_references:
      default: "true"
      description: "Whether to ignore external references in discovered attestations"
    ignore_upstream_attestations:
      default: "true"
      description: "Whether to ignore attestations from upstream images"

    # Job Configuration
    runner_tags:
      default: []
      type: array
      description: "The runner tags to use for the job"
    pull_policy:
      default: "always"
      description: "The pull policy for the Docker image"
    allow_failure:
      default: false
      type: boolean
      description: "Whether the jobs are allowed to fail without stopping the pipeline"
    registry:
      default: "$CI_REGISTRY"
      description: "Container registry URL"
    registry_user:
      default: "$CI_REGISTRY_USER"
      description: "Container registry username"
    registry_password:
      default: "$CI_REGISTRY_PASSWORD"
      description: "Container registry password"
    git_strategy:
      default: "fetch"
      description: "The Git strategy to use for the job"
    # Component version
    version:
      default: "main"
      description: "Version/ref of the components to use"
    # Image tags to si
    # Supply chain ID for build provenance
    supplyChainId:
      default: "$CI_COMMIT_SHA"
      description: "Supply chain id forwarded to build provenance / supply-chain artifacts"
    path:
      description: "Path to the Containerfile/Dockerfile"
    predicate_type:
      default: ""
      description: "The predicate type to discover. If empty, all attestations will be discovered."
---
include:
  - remote: "https://gitlab.com/l3montree/devguard/-/raw/$[[ inputs.version ]]/templates/discover-baseimage-attestations.yml"
    inputs:
      stage: $[[ inputs.build_stage ]]
      predicate_type: $[[ inputs.predicate_type ]]
      path: $[[ inputs.path ]]
      registry_user: $[[ inputs.registry_user ]]
      registry_password: $[[ inputs.registry_password ]]
      allow_failure: true
      registry: $[[ inputs.registry ]]
      job_suffix: $[[ inputs.job_suffix ]]

  - remote: "https://gitlab.com/l3montree/devguard/-/raw/$[[ inputs.version ]]/templates/container-lifecycle.yml"
    inputs:
      build_stage: $[[ inputs.build_stage ]]
      scan_stage: $[[ inputs.scan_stage ]]
      deploy_stage: $[[ inputs.deploy_stage ]]
      devguard_token: $[[ inputs.devguard_token ]]
      devguard_api_url: $[[ inputs.devguard_api_url ]]
      devguard_asset_name: $[[ inputs.devguard_asset_name ]]
      devguard_artifact_name: $[[ inputs.devguard_artifact_name ]]
      devguard_web_ui: $[[ inputs.devguard_web_ui ]]
      devguard_origin: $[[ inputs.devguard_origin ]]
      fail_on_cvss: $[[ inputs.fail_on_cvss ]]
      fail_on_risk: $[[ inputs.fail_on_risk ]]
      build_args: $[[ inputs.build_args ]]
      push_image: $[[ inputs.push_image ]]
      image_suffix: $[[ inputs.image_suffix ]]
      ignore_upstream_attestations: $[[ inputs.ignore_upstream_attestations ]]
      ignore_external_references: $[[ inputs.ignore_external_references ]]
      runner_tags: $[[ inputs.runner_tags ]]
      pull_policy: $[[ inputs.pull_policy ]]
      allow_failure: $[[ inputs.allow_failure ]]
      git_strategy: $[[ inputs.git_strategy ]]
      version: $[[ inputs.version ]]
      job_suffix: $[[ inputs.job_suffix ]]

  - remote: "https://gitlab.com/l3montree/devguard/-/raw/$[[ inputs.version ]]/templates/vex-upload.yml"
    inputs:
      stage: $[[ inputs.deploy_stage ]]
      devguard_token: $[[ inputs.devguard_token ]]
      devguard_api_url: $[[ inputs.devguard_api_url ]]
      devguard_asset_name: $[[ inputs.devguard_asset_name ]]
      devguard_artifact_name: $[[ inputs.devguard_artifact_name ]]
      devguard_origin: BASE_IMAGE_VEX
      runner_tags: $[[ inputs.runner_tags ]]
      pull_policy: $[[ inputs.pull_policy ]]
      allow_failure: true
      git_strategy: $[[ inputs.git_strategy ]]
      job_suffix: $[[ inputs.job_suffix ]]
      # hopefully we were able to download something - if not, we allow_failure
      vex_file: vex.json
      needs:
        - devguard:discover_baseimage_attestations$[[ inputs.job_suffix ]]
      dependencies:
        - devguard:discover_baseimage_attestations$[[ inputs.job_suffix ]]

  - remote: "https://gitlab.com/l3montree/devguard/-/raw/$[[ inputs.version ]]/templates/sbom-upload.yml"
    inputs:
      stage: $[[ inputs.deploy_stage ]]
      devguard_token: $[[ inputs.devguard_token ]]
      devguard_api_url: $[[ inputs.devguard_api_url ]]
      devguard_asset_name: $[[ inputs.devguard_asset_name ]]
      devguard_artifact_name: $[[ inputs.devguard_artifact_name ]]
      devguard_origin: BASE_IMAGE_SBOM
      runner_tags: $[[ inputs.runner_tags ]]
      pull_policy: $[[ inputs.pull_policy ]]
      allow_failure: true
      git_strategy: $[[ inputs.git_strategy ]]
      job_suffix: $[[ inputs.job_suffix ]]
      # hopefully we were able to download something - if not, we allow_failure
      sbom_file: sbom.json
      needs:
        - devguard:discover_baseimage_attestations$[[ inputs.job_suffix ]]
      dependencies:
        - devguard:discover_baseimage_attestations$[[ inputs.job_suffix ]]
